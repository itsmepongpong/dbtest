<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Dimmed background shown behind the panel -->
    <View
        android:id="@+id/scrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#99000000"
        android:visibility="gone" />

    <!-- Sliding Panel -->
    <LinearLayout
        android:id="@+id/buildingPanel"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:orientation="vertical"
        android:background="#FFFFFF"
        android:elevation="12dp"
        android:padding="20dp">

        <!-- Building Name -->
        <TextView
            android:id="@+id/panelBuildingName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Building Name"
            android:textColor="#000000"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Status Indicator -->
        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Status: Available"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#4CAF50" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:background="#E0E0E0" />

        <!-- Reserve Button (Visible when Available) -->
        <Button
            android:id="@+id/btn_reserve"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/building"
            android:text="Reserve"
            android:textColor="#FFFFFF" />

        <!-- Already Reserved Indicator (Visible when Reserved) -->
        <LinearLayout
            android:id="@+id/layout_already_reserved"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="#F5F5F5"
            android:padding="12dp"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🔒 Already Reserved"
                android:textColor="#757575"
                android:textSize="16sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Close Button -->
        <Button
            android:id="@+id/btn_close_panel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:backgroundTint="#E0E0E0"
            android:text="Close"
            android:textColor="#000000" />

    </LinearLayout>

</FrameLayout>